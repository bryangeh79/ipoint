# Session Memory — July 16, 2026

## Identity & Context
- **Model:** deepseek/deepseek-v4-flash (this session)
- **Role:** OpenClaw (project general manager — does not write code)
- **Execution Engine for code work:** Codex CLI v0.144.4
- **Auth Source:** CHATGPT_ACCOUNT_SESSION
- **Codex Model:** gpt-5.6-sol (used because DeepSeek not supported with ChatGPT auth)
- **Human:** Bryan (final business owner)
- **Command Center:** ChatGPT Command Center (product commander, architecture authority, acceptance reviewer)
- **Workspace:** C:\AI_WORKSPACE\iPoint App
- **Repository:** bryangeh79/ipoint
- **Current Branch:** phase/0-engineering-foundation
- **PR #4:** OPEN (Draft) — Phase 0: Engineering Foundation

## Critical Policy Lessons Learned (must not repeat)
1. **CODEX-ONLY EXECUTION POLICY:** Engineering work must ONLY be done by real Codex CLI processes. No OpenClaw sub-agents, no `sessions_spawn`, no delegated agents.
2. **Sub-agent violation (D-005):** P0-S3 and P0-S4A were initially executed via OpenClaw sub-agents. All output was invalidated as UNTRUSTED_SUBAGENT_OUTPUT. Codex CLI had to redo both phases.
3. **Codex CLI communication:** Codex CLI does NOT auto-report completion. OpenClaw must actively poll. The user got frustrated waiting. Fix: implement active polling loop when Codex CLI workers are running.
4. **Package corruption:** `npm init -y` accidentally rewrote root `package.json`, adding junk deps. Had to regenerate `pnpm-lock.yaml`.
5. **Git index corruption:** isomorphic-git's raw .git manipulation left the index checksum-mismatched. Had to delete and rebuild index.
6. **Model restriction:** Codex CLI with ChatGPT auth only supports OpenAI models, not DeepSeek.

## Completed Work (approved)

### P0-S1: Repository Audit
- Workspace classification and cleanup rules established
- .gitignore corrected (removed tasks/, cleaned OpenClaw block)
- Governance: Tracked only approved engineering files

### P0-S2: Monorepo Application Shells
- Root toolchain: .editorconfig, .prettierrc.json, eslint.config.mjs, tsconfig.json, vitest.config.ts, playwright.config.ts
- 6 shared packages: business-rules, config, design-tokens, types, ui, validation
- 4 app shells: api (TypeScript), member-web (React+Vite), merchant-web (React+Vite), admin-web (React+Vite)
- `apps/api`: NestJS backend foundation (verified by Codex CLI, `259c3a34`)
  - /api/v1 global prefix
  - GET /health/live, GET /health/ready (no DB check)
  - Config module with @ipoint/config fail-fast
  - Zod validation pipe, standard error format
  - x-request-id middleware (accept/generate/return/log)
  - Structured logging (nestjs-pino)
  - OpenAPI/Swagger at /api/v1/docs
  - Graceful shutdown + unit/integration tests (31/31)

### ORM Gate (D-006)
- **Decision:** APPROVE_DRIZZLE
- **Evidence:** `experiments/orm-comparison/` (52 files, full PoC with both ORMs)
- **Verified under Node 26.4.0** (UNVALIDATED_ON_TARGET_NODE_LTS — Node 24 not available on host)
- **Prisma evaluated but NOT selected**
- **ORM Gate CLOSED — DRIZZLE APPROVED**
- PostgreSQL remains source of truth. Exact decimal via PostgreSQL numeric.
- Production migrations: explicit reviewable SQL, forward-fix recovery, immutable checksums.

### P0-S4B: Database Foundation (COMMITTED to phase branch)
- Package: `packages/database/ @ipoint/database`
- ORM: Drizzle v0.45.2 + pg v8.22.0
- **14 entities with full constraints:**
  - accounts (UUID PK, public_id unique, email check lowercase, country_code=2)
  - credentials (secret_hash, hash_algorithm+version, revoked_at)
  - sessions (family_id, access/refresh token hashes only, rotation tracking, expiry checks)
  - otps (code_hash=64, attempts bounded, purpose enum, one-time use)
  - security_events (JSONB metadata, account+time index)
  - markets (code uppercase 2-8, currency_code=3, IANA timezone)
  - admin_users (1:1 with account, status tracking)
  - roles (isSystem flag, code unique)
  - permissions (code unique, description required)
  - role_permissions (junction table, PK composite)
  - role_assignments (active-unique partial index where revoked IS NULL)
  - market_access (same active-unique partial index pattern)
  - audit_logs (actor/action/entity/before-after JSONB, append-only design)
  - entity_timelines (read model, entity+time index)
- **6 enums:** account_status (PENDING/ACTIVE/SUSPENDED/LOCKED/ARCHIVED), credential_type (PASSWORD), otp_purpose, market_status, admin_status, actor_type, event_result
- **Migration infrastructure:** drizzle-kit generated SQL, checksum validation, drift detection, from-zero rebuild
- **Tests:** Schema unit tests (entity count, hash-only columns, required constraints) + integration tests (migration, seed idempotency, drift, append-only audit)
- **ADR-006:** Drizzle Database Foundation decision record

### P0-S5 + P0-S6: NOT YET IMPLEMENTED
These were specified to be service-layer packages but do not exist on disk yet:
- No `packages/auth/` (auth service, guards, session management)
- No `packages/authorization/` (RBAC evaluation, audit service)
- No ADR-007 or ADR-008

### Batch A Pending Items
1. P0-S5 Auth Foundation (service layer: credential validation, session CRUD, OTP workflow, password reset, auth guards, rate limit interface)
2. P0-S6 RBAC/Audit Foundation (service layer: role/permission evaluation, market scope check, audit logging service, entity timeline)
3. Node 24 LTS validation (blocked: host has Node 26.4.0 only)
4. PR #4 description update with Batch A results
5. Full verification passing

### Branch Structure
- `phase/0-engineering-foundation` (main Phase 0 branch) — latest: committed P0-S4B
- `task/p0-s3-backend-foundation` (PUSHED: `259c3a34`)
- `task/p0-s4a-orm-comparison` (local only, CORRUPTED by sub-agent, preserved for evidence)
- `task/p0-s4a-orm-comparison-clean` (PUSHED: `b805d6c9`)
- `task/p0-s4a-orm-poc` (PUSHED: `56d36ef6`)

## Prohibitions
- ❌ No OpenClaw sub-agents for engineering work
- ❌ No API keys (OPENAI_API_KEY, CODEX_API_KEY, OPENAI_TOKEN, CODEX_AUTH_TOKEN)
- ❌ Only CHATGPT_ACCOUNT_SESSION for auth
- ❌ No push to main
- ❌ No merge of PR #4
- ❌ No P0-S7 start until Batch A is audited by Command Center
# Session Memory — July 16, 2026

## Identity & Context
- **Model:** deepseek/deepseek-v4-flash (this session)
- **Role:** OpenClaw (project general manager — does not write code)
- **Execution Engine for code work:** Codex CLI v0.144.4
- **Auth Source:** CHATGPT_ACCOUNT_SESSION
- **Codex Model:** gpt-5.6-sol (used because DeepSeek not supported with ChatGPT auth)
- **Human:** Bryan (final business owner)
- **Command Center:** ChatGPT Command Center (product commander, architecture authority, acceptance reviewer)
- **Workspace:** C:\AI_WORKSPACE\iPoint App
- **Repository:** bryangeh79/ipoint
- **Current Branch:** main (Phase 0 merged and closed)

## Critical Policy Lessons Learned (must not repeat)
1. **CODEX-ONLY EXECUTION POLICY:** Engineering work must ONLY be done by real Codex CLI processes. No OpenClaw sub-agents, no `sessions_spawn`, no delegated agents.
2. **Sub-agent violation (D-005):** P0-S3 and P0-S4A were initially executed via OpenClaw sub-agents. All output was invalidated as UNTRUSTED_SUBAGENT_OUTPUT. Codex CLI had to redo both phases.
3. **Codex CLI communication:** Codex CLI does NOT auto-report completion. OpenClaw must actively poll. The user got frustrated waiting. Fix: implement active polling loop when Codex CLI workers are running.
4. **Package corruption:** `npm init -y` accidentally rewrote root `package.json`, adding junk deps. Had to regenerate `pnpm-lock.yaml`.
5. **Git index corruption:** isomorphic-git's raw .git manipulation left the index checksum-mismatched. Had to delete and rebuild index.
6. **Model restriction:** Codex CLI with ChatGPT auth only supports OpenAI models, not DeepSeek.

## Completed Work (approved)

### P0-S1: Repository Audit
- Workspace classification and cleanup rules established
- .gitignore corrected (removed tasks/, cleaned OpenClaw block)
- Governance: Tracked only approved engineering files

### P0-S2: Monorepo Application Shells
- Root toolchain: .editorconfig, .prettierrc.json, eslint.config.mjs, tsconfig.json, vitest.config.ts, playwright.config.ts
- 6 shared packages: business-rules, config, design-tokens, types, ui, validation
- 4 app shells: api (TypeScript), member-web (React+Vite), merchant-web (React+Vite), admin-web (React+Vite)
- `apps/api`: NestJS backend foundation (verified by Codex CLI, `259c3a34`)
  - /api/v1 global prefix
  - GET /health/live, GET /health/ready (no DB check)
  - Config module with @ipoint/config fail-fast
  - Zod validation pipe, standard error format
  - x-request-id middleware (accept/generate/return/log)
  - Structured logging (nestjs-pino)
  - OpenAPI/Swagger at /api/v1/docs
  - Graceful shutdown + unit/integration tests (31/31)

### ORM Gate (D-006)
- **Decision:** APPROVE_DRIZZLE
- **Evidence:** `experiments/orm-comparison/` (52 files, full PoC with both ORMs)
- **Prisma evaluated but NOT selected**
- **ORM Gate CLOSED — DRIZZLE APPROVED**
- PostgreSQL remains source of truth. Exact decimal via PostgreSQL numeric.
- Production migrations: explicit reviewable SQL, forward-fix recovery, immutable checksums.

### P0-S4B: Database Foundation
- Package: `packages/database/ @ipoint/database`
- ORM: Drizzle ORM + pg driver
- **14 entities with full constraints** (accounts through entity_timelines)
- **6 enums** (account_status, credential_type, otp_purpose, market_status, admin_status, actor_type, event_result)
- 2 migrations (`0000_database_foundation.sql`, `0001_auth_session_access_expiry.sql`)
- Migration checksums, drift detection, append-only triggers
- Tests: schema unit + integration (migration, seed, drift, append-only audit)

### P0-S5: Auth Foundation
- In `apps/api/src/auth/`
- Password hashing (scrypt Node built-in)
- Session management with refresh token rotation (families)
- OTP: hash-only storage, bounded attempts, one-time use
- Security events with redaction (PII automatically stripped)
- Auth guards (ApiKeyGuard, SessionGuard), rate limiter interface (in-memory, non-production)

### P0-S6: Market/RBAC/Audit Foundation
- In `apps/api/src/platform-access/`
- Market registry (Zod-validated CRUD)
- RBAC evaluation (deny-by-default, market-scoped, no header/request trust)
- Audit service with redaction patterns, entity timeline service

### P0-S7: Design System Foundation
- 30 accessible UI components (Button through ConfirmationDialog)
- Official iPoint design tokens: primary green #20B366, gold secondary
- Typography: Inter (Latin) + Noto Sans SC/TC + Noto Sans SEA
- 8pt spacing, WCAG 2.1 AA foundations
- ADR-007

### P0-S8: CI/Testing/Local Environment
- `.github/workflows/ci.yml` — 5 jobs: quality, unit-tests, database-tests, api-tests, e2e
- Root scripts (db:migrate, db:seed, db:checksum, db:drift, ci:verify)
- compose.yaml, .env.example
- GitHub Actions Run ID 29484314355 — all 5 jobs SUCCESS

### P0-S9: Phase 0 Final Integration & Acceptance
- Full-scope integration audit: 22 commits, 203 files, 0 PROHIBITED, 0 UNEXPECTED
- Fresh detached-HEAD verification: 71 unit + 49 API + 11 database + 1 E2E tests ALL PASSING
- Migration, checksum, seed-idempotency, drift gates ALL PASSING
- PHASE 0 FINAL ACCEPTANCE REPORT created

## Phase 0 Closure (D-009) — 2026-07-16 17:32-17:56
- **APPROVED by ChatGPT Command Center**
- **Final Phase Head:** `f9706c4bd4719a180cd83953d3c1ca94bda9eef4`
- **PR #4:** Draft → Ready for Review → SQUASH MERGED into main
- **Merge commit:** `46912b557227954e392ed622189eda82892cd717`
- **Governance close commit:** `f2c2964725d65309e44068e3c6124dc955fa8114`
- **Main CI:** Run 29487989585 — all 5 jobs SUCCESS
- **D-009:** Phase 0 Engineering Foundation Accepted — recorded in DECISION_LOG.md
- **PHASE_REGISTRY:** Phase 0 APPROVED + CLOSED

## Final State
- **Branch:** main (`46912b557227954e392ed622189eda82892cd717`)
- **PR #4:** MERGED (squash)
- **Phase 1:** NOT AUTHORIZED — requires new chat session + Command Center authorization
- **Untracked local dirs preserved:** .acceptance/, .acceptance-evidence/, .local/, Concept/, DECISIONS.md, GEMINI.md, memory/, scripts/, tasks/
- **No business logic merged into main**
- **Design tokens:** Green #20B366 primary, gold secondary
- **Database:** Drizzle + PostgreSQL with 14 entities

## Key Decisions Made
- **D-006:** Drizzle ORM approved for production baseline (ORM Gate CLOSED)
- **D-007:** Batch A accepted (P0-S4B → P0-S5 → P0-S6)
- **D-008:** Batch B accepted (P0-S7 → P0-S8), P0-S9 authorized
- **D-009:** Phase 0 Engineering Foundation Accepted and CLOSED

## Prohibitions
- ❌ No OpenClaw sub-agents for engineering work
- ❌ No API keys
- ❌ Only CHATGPT_ACCOUNT_SESSION for auth
- ❌ No Phase 1 work until new authorization in new chat session
- ❌ No Merchant business schema modifications
- ❌ No Merchant onboarding implementation
