# TASK_BRIEF_P8S7 — Backup / Restore / Monitoring / Security Readiness (G-07 + F-02)

> Phase 8 · Sub-phase **P8-S7** · Branch `task/p8-s7-backup-restore-monitoring-security` (based on `phase/8-final-delivery-readiness` HEAD)
> Contract: `P8_S0_CONTRACT_FREEZE.md` §7 (G-07) + §0/§11/§12 — D-058 §17 domain, as frozen 2026-08-08
> Gap audit: `P8_S0_GAP_AUDIT_REPORT.md` row **G-07** (Backup / Restore / Monitoring / Security — own gap row) + **F-02** (Redis prerequisite: E-03 PMC + D-019-C hard production prerequisite; PostgreSQL remains source of truth)
> Executor: independent coding subagent (D-060 alternate executor authorization; **Codex CLI regains implementation priority when available per D-058 §5 — production code is CODEX-CLI-ONLY unless D-060 applies**) · Reviewer: independent Reviewer B' (D-060) · Verifier: OpenClaw host integration gate
> Risk class: **HIGH** (infra + ops + security; contract freeze §7 lists backup/restore and production-readiness controls in the A→B→C mandatory domains) → implementer → independent reviewer → independent verifier; implementer never self-approves

---

## 1. Mission

Deliver the **verifiable Backup / Restore / Monitoring / Security Readiness evidence package for iPoint V1 at Phase 8 state**, per contract §7 (G-07) + gap audit F-02:

1. **DB + migration integrity**: fresh and upgrade migration rehearsal (checksums 40/40 frozen — rehearsal only, zero new migrations), DB integrity verification.
2. **Backup procedure + actual restore test**: documented procedure, executed backup → restore → verify rehearsal on host PostgreSQL with a dedicated test database, plus DR evidence and recovery procedures.
3. **Monitoring / structured logs / redaction / alerts**: honest, template-form monitoring surface (metrics, logs, alert rules, dashboard templates) that **can observe the known OBS-04 High limitation class** (idle-in-transaction / connection-pool stall on the reconciliation path) — monitoring must make it visible, not pretend it does not exist.
4. **Rate limiting via Redis** (F-02): introduce Redis **behind existing service boundaries** — distributed rate limiter (closing P2-S4 AHS-003 / D-019-C), distributed lock and queue primitives for cross-instance coordination only; **PostgreSQL remains the source of truth** and no PG-based correctness mechanism (advisory locks, idempotency, outbox exactly-once) may be replaced or weakened.
5. **Runtime / dependency / environment validation + health/readiness checks**: verify and, where in contract scope, extend so startup and probes validate configuration and dependencies.
6. **Recovery procedures, operator/incident runbooks, release checklist**: checked-in ops documentation per `docs/04-engineering/06_iPoint_Deployment_Security_and_Operations_V1.0.md` §7/§8/§10/§12.
7. **Security readiness**: secret scan clean (repo + test fixtures + evidence), dependency checks, zero-owner-bypass re-scan, relationship to the closed SEC-01/SEC-02/P6-R2/P2-S4 security work — with **no secrets and no production credentials anywhere** (production credentials are classified deployment blockers, not deliverables, per contract §7).

This is an **evidence + bounded-production-code** sub-phase: production code changes are authorized **only** within the frozen contract §7 surface (Redis boundary introduction, health/readiness extension, rate-limiter integration) and only by the authorized executor class (§0 header). Everything else is documentation + host-run tooling + evidence. No feature work. No new migrations.

## 2. Background — what S5a..S6 already closed (do NOT redo)

| Sub-phase | Closed | Evidence |
|---|---|---|
| S5a | admin-web renders all 19 reports (R01–R19), freshness honesty, no-export | `P8_S5A_FINAL_GATE_RECORD.md` (merge `e701ebd3`), admin-web 336/336 |
| S5b | member-web Wallet / Reward / Team / Redemption pages over frozen Phase 3/5/6 backends | `P8_S5B_FINAL_GATE_RECORD.md` (merge `a71d0a46`), member-web 311/311 |
| S5c | merchant-web transactions preview/confirm/receipt/history over frozen Phase 4 endpoints | `P8_S5C_FINAL_GATE_RECORD.md` (merge `7fe4ed28`), merchant-web 24/24 + api-client 100/100 |
| S5d | full-repo Phase 8 CI workflow `.github/workflows/p8-ci.yml` (6 jobs, fail-closed guarded suites S1–S4/S6 against dedicated `ipoint_p8sN_*` DBs, never `ipoint_ci`; **redis:7-alpine service already provisioned in the api-integration job** + `REDIS_URL` env) | `P8_S5D_FINAL_GATE_RECORD.md` (merge `eb83daaf`), D-066 |
| S5e | cross-platform consistency matrix (11 dimensions × all surfaces, all CONSISTENT), F-04 controller map (48 controllers all guarded, RBAC 49/49), F-06 drift set re-verified, zero-owner-bypass scan 445 files / 0 direct bypass, OpenAPI 285 paths | `P8_S5E_FINAL_GATE_RECORD.md` (merge `b043e932`), D-068 |
| S6 | load/performance/concurrency evidence: 12-journey L0/L1/L2 matrix (0 unexpected errors at every reported level), storm assertion set per write journey, retry-site table (0 unbounded), DB/worker observations, FIX-001..004 bounded fixes, additive L0 CI smoke; **OBS-04 documented HIGH residual** (see §2.1) | `P8_S6_LOAD_PERFORMANCE_CONCURRENCY_REPORT.md`, `P8_S6_FINAL_GATE_RECORD.md` (merge `dc6a69ee`), D-069/D-070 |

P8-S7 therefore covers **ONLY** contract §7 (G-07) + gap audit F-02. It does **not** re-run S5e's consistency matrix or scans except where S7 touches production code (then re-scan the changed scope), does **not** re-do S6 load storms, and does **not** build any new business feature.

### 2.1 OBS-04 — the known HIGH limitation the monitoring deliverable MUST observe

- **OBS-04 (High, OPEN — escalated, D-070):** idle-in-transaction connection-lifecycle stall on the **reconciliation path** — sessions stuck "idle in transaction / ClientRead" on ALL pooled connections (10/10 observed via `pg_stat_activity`) holding `UPDATE reconciliation_runs`; requests block on the pool queue; no statement/lock timeout can fire because no statement is executing (client idle). FIX-002 (10s/3s on `withIdempotency`) bounds waiters but cannot prevent the class; FIX-004 closed the adjust-chain (J3/J9) sub-class; the engine-level residual (pool acquire timeout / tx-scoped connection discipline / driver tracing; FOR UPDATE OF narrowing) is **a Command Center decision** (D-070 named remediation paths, Option A P8-S2-domain bounded fix / Option B documented-risk precedent). **Resolution or formal Bryan acceptance is REQUIRED before the Phase 8 final gate (D-058: "0 unresolved HIGH").**
- **Consequence for S7 acceptance framing (§1.5 of the S6 brief analog):** S7 monitoring/alerting must include **detectable indicators for this exact class** — idle-in-transaction age, pool saturation, `pg_stat_activity` snapshot/alert capability, reconciliation `run_execute` stall alerts — and demonstrate on host that the indicator set fires on a reproduced stall (OBS-04 reproduction ledger exists: `apps/api/.local/p8-s6-load/2026-08-10T08-44Z-j10-l2-rerun-stall/OBSERVATION.md`). **S7 does NOT remediate OBS-04** — remediation is out of S7 scope (reconciliation engine + connection discipline belong to the P8-S2 domain / Command Center decision). S7 makes it observable and documents the dependency on the Command Center decision in the delivery report.
- **OBS-01/02/03/05 (Medium/Low, documented):** quote ms-granular idempotency key (409-consumed race, safe/bounded), reconciliation lock-timeout waiters surfacing as 500 (55P03, Low), outbox "Dispatch failed: not CONFIRMED" log (expected business rejection), deprecated recharge route 403 by design — all documented in the S6 discrepancy log; S7 alert design should reference these so alert rules do not false-positive on expected outcomes.

### 2.2 Existing security / ops evidence — REUSE, do not regenerate

| Evidence | Scope | Location | Reuse in S7 |
|---|---|---|---|
| P7-S10 gate 19 — no secret exposure (only `.env.example` placeholders) + gate 20 — 0C/0H security review verdicts | Repo-wide, Phase 7 state | `docs/06-phase-reports/p7-s10/P7-S10_FINAL_GATE_RECORD.md` | **Security-baseline claim** for §3.7; S7 re-runs the secret scan at Phase 8 state (incl. P8 code + S7 evidence files) |
| P2-S4 auth security review — AHS-001 (High, fixed via atomic SQL increment), AHS-003 (Medium: `InMemoryRateLimiter` does not scale across instances — Redis is the named fix), AHS-002/004 (Medium, documented) | Auth domain | `docs/05-security/auth-security-review.md` | **Direct contract link**: F-02/D-019-C Redis rate limiter closes AHS-003; ceilings (10/IP/300s, 5/email/300s) are frozen CONFIGURABLE values that the Redis limiter must preserve |
| SEC-01 / SEC-02 / P6-R2 hardened routes (closed, frozen) | Admin route security, refund ledger owner | `docs/06-phase-reports/p7-sec01/SEC01_FINAL_GATE_RECORD.md`, `docs/06-phase-reports/p7-sec02/SEC02_FINAL_GATE_RECORD.md`, `P6R2_ADMIN_ROUTE_SECURITY_FINAL_GATE_RECORD.md` | **Do-Not-Touch owners** + the security-readiness "already closed" narrative in §3.7 |
| Ops engineering doc — §7 Observability (structured logs/metrics/alerting for auth failures, error rate/latency, DB+queue health, tx/MCP failures, reward settlement, reconciliation differences, admin privileged actions; logs must include request ID, service/module, env, market context), §8 Backup/recovery (automated PG backups, PITR where supported, restore tests on a schedule, documented RTO/RPO before production launch, ledger reconciliation after recovery), §10 Incident management (severity levels + runbooks: security incident, incorrect ledger balance, duplicate transaction, settlement failure, payment mismatch, data exposure, production outage), §12 production-readiness evidence list | Ops policy | `docs/04-engineering/06_iPoint_Deployment_Security_and_Operations_V1.0.md` | **Authoritative template** for the S7 runbooks, release checklist, backup/RTO/RPO and monitoring inventory — S7 evidence must map to these sections |
| Existing health endpoints — `GET /health/live` (liveness) + `GET /health/ready` (readiness; currently only `checks.config: 'ok'` — no DB/Redis dependency check) | API | `apps/api/src/health/health.controller.ts` (+ `.spec.ts`, `.module.ts`) | **Baseline for §3.5** — verify + bounded extension to dependency checks (contract AC: "health/readiness endpoints") |
| Existing structured-log redaction — `safeErrorMetadata` / `safeRequestRoute` with SAFE_DATABASE_CODES whitelist; pino structured logging in api only | API common | `apps/api/src/common/logging/log-redaction.ts`, `apps/api/src/common/logging/` | **Baseline for §3.3 redaction verification** — S7 audits coverage (tokens, OTP, KYC, secrets, full credentials must never reach logs) |
| Rate-limit port abstraction — `RateLimitPort.consume(key, limit, windowSeconds)` + `InMemoryRateLimiter`; `AUTH_RATE_LIMITER` provider via `useClass` | Auth | `apps/api/src/auth/rate-limit.port.ts`, `auth.module.ts` | **Integration point for the Redis limiter (F-02)** — implement `RedisRateLimiter` behind the same port; frozen ceilings unchanged |
| Config/env plumbing — `ConfigService.redisUrl` getter + `.env.example` `REDIS_URL`/`REDIS_PORT`; `DATABASE_URL` runtime validation exists | API | `apps/api/src/config/config.service.ts`, `.env.example` | **Redis module config surface already exists** — S7 adds the client + validation, no new env names needed beyond documented ones |
| P8-S6 DB/worker observation methods — `pg_stat_activity` snapshots, `pg_stat_database` deadlock/rollback counters, pool behaviour, outbox drain, EXPLAIN method | Phase 8 state | `P8_S6_LOAD_PERFORMANCE_CONCURRENCY_REPORT.md` §7/§9/§10 + `.local/p8-s6-load/*-db-observations/` | **Monitoring indicator design**: S7 alert/metric inventory is built on indicators S6 already proved measurable (pool saturation, idle-in-transaction, deadlock deltas, outbox backlog) |
| Zero-owner-bypass scan method + baseline (445 files / 0 bypass; scanner `scan-zerobypass.mjs` under `.local/p8-s5e/`) | Phase 8 state | `P8_S5E_DELIVERY_REPORT.md`, `P8_S5E_FINAL_GATE_RECORD.md` | **Re-run over S7-changed production scope** (Redis module, health, limiter) — assert 0 findings; otherwise cite as unchanged baseline |
| Migration state — 0000–0039 + `checksums.json`, **40/40 frozen**; F-08 clean state | Repo-wide | `packages/database/migrations/`, `P8_S0_GAP_AUDIT_REPORT.md` F-08 | **Rehearsal-only target** for §3.2 — fresh + upgrade rehearsal must end with checksums 40/40 untouched |
| P8-S0 untracked baseline — 119 files official (secret-free per F-05; local test/POC connection strings only, not production, not a blocker) | Repo-wide | `P8_S0_UNTRACKED_CLASSIFICATION.md`, gap audit F-05 | **Do-not-bulk-add**; S7 evidence files go ONLY to `docs/06-phase-reports/p8-s7/` + the authorized code locations (§8) |

## 3. Scope — contract §7 (G-07 + F-02), item by item

Contract §7 scope text (verbatim): *"DB integrity, migration integrity (fresh + upgrade rehearsal), backup procedure + actual restore test, monitoring, structured logs, redaction, alerts, rate limiting, runtime validation, dependency checks, environment validation, health/readiness checks, recovery procedures, operator/incident runbooks, release checklist, DR evidence; introduce Redis behind service boundaries (distributed rate limiter/locks/queue — E-03/D-019-C prerequisite; PostgreSQL remains source of truth)."*

AC (verbatim): *"restore rehearsal evidence (backup → restore → verify), migration fresh+upgrade rehearsal, health/readiness endpoints, alert rules, runbooks/release checklist docs, Redis integration tests (limiter/lock), secret scan clean."*

### 3.1 DB integrity + backup procedure + actual restore test + DR evidence (documentation + host-run evidence)

- **DB integrity baseline**: capture current integrity posture — checksums 40/40, `db:drift` clean (re-run), FK/index sanity on host PG (reuse P7-S10 gate 1 / p8-ci `database` job commands). This is the "before" state the backup/restore evidence is measured against.
- **Backup procedure** (doc, mapping to ops doc §8): host-run `pg_dump`-based procedure (custom format recommended) with naming/retention convention, documented in the report; **PITR (`pg_basebackup` + WAL archiving) documented as a production-time capability — not rehearsed on host** (wal_archive setup is a deployment concern; flag as deployment-blocker note, do not fake it).
- **Actual restore test — rehearsal on host PG, dedicated test DBs only**: backup an `ipoint_p8s7_source_*` database (created via `db:migrate` + `db:seed` at Phase 8 state), restore into a fresh `ipoint_p8s7_restore_*` database, and **verify**: row-count/checksum spot checks on representative tables (ledger, transactions, members, migrations table), `db:drift` clean on the restored DB, and a functional smoke (health endpoint + one read query + one write-path round trip on the restored DB in a disposable test environment). Evidence: commands, dump metadata, verification outputs — raw under gitignored `.local/p8-s7-backup/**`, summarized in the report (S6 honesty contract: never fabricate a restore; a failed restore is reported as such).
- **DR evidence**: DR narrative (what would be restored, in what order, to what target), RTO/RPO **documented as targets** — see O-5 (values are CONFIGURABLE/decision items, not hard-coded inventions); ledger-reconciliation-after-recovery step per ops doc §8 (reference P8-S2 reconciliation run as the post-restore verification tool).
- **Host-only constraint**: restore rehearsal runs on the host (Windows, PostgreSQL 17 per P4-S7 environment `127.0.0.1:55440` or S6 compose container `127.0.0.1:55432` — S7 uses its own dedicated `ipoint_p8s7_*` DBs, never `ipoint_ci`, never any shared dev DB). No production database is touched — **no production deployment/backup authorization exists; production backup is a deployment-blocker note, not a deliverable**.

### 3.2 Migration integrity — fresh + upgrade rehearsal (host-run evidence)

- **Fresh rehearsal**: apply 0000–0039 to an empty dedicated `ipoint_p8s7_fresh_*` DB; verify all 40 apply cleanly, `checksums.json` match, `db:drift` clean, seed succeeds.
- **Upgrade rehearsal**: apply a staged subset (e.g., the Phase 7 head 0036 set) then apply the P8 additions 0037–0039; verify forward-only application, no drift, checksums 40/40 unchanged after both rehearsals.
- **Zero new migrations** (frozen 40/40); the rehearsals are evidence runs only, using existing `pnpm db:migrate` / `pnpm db:checksum` / `pnpm db:drift` tooling (p8-ci `database` job shape). Deliverable: `P8_S7_MIGRATION_REHEARSAL_REPORT.md` + raw outputs under `.local/p8-s7-migration/**`.

### 3.3 Monitoring / structured logs / redaction / alerts (template + verification form, no deployed infra)

**Landing-form principle (no production deployment authorization):** all monitoring/alerting deliverables are **checked-in templates, scripts and documentation**, validated on host where possible — never deployed infrastructure, never a claim of live production monitoring. This keeps S7 self-consistent with the "no deployment" constraint while satisfying the AC ("alert rules", "monitoring").

- **Metric inventory** (mapping to ops doc §7): define the indicator set per category — auth failures/abuse, API error rate/latency, DB + queue health, transaction/MCP/reward-settlement failures, reconciliation differences, admin privileged actions, **plus the OBS-04 class indicators** (§2.1: idle-in-transaction age, pool saturation %, `pg_stat_activity` stall snapshot, reconciliation run_execute stall). Every metric carries: definition, source query/command (S6-proven measurement methods §7 — `pg_stat_database` deadlock/rollback deltas, `pg_stat_activity` snapshots, outbox backlog counts), and alert threshold proposal (thresholds are CONFIGURABLE — see O-5; S7 proposes, does not hard-code into production).
- **Structured logs + redaction verification**: audit the existing pino + `log-redaction.ts` surface at Phase 8 state — assert no token/OTP/KYC/secret/credential reaches logs (static scan + runtime spot check via existing integration suites); document any gap (severity-classified; bounded fix only with a written fix record per §11, or documented-only if Low). Log requirements from ops doc §7 (request ID, service/module, env, market context) verified against actual log output.
- **Alert rules** (deliverable form): committed **alert-rule templates** — e.g., Prometheus-compatible alerting-rules YAML as configuration templates (no new runtime dependency; templates are config artifacts) covering the metric inventory, each with expr proposal, severity (Critical/High/Medium/Low per the S8 severity guide), and the runbook reference. **Alert design must reference the S6 expected-outcome catalogue** (OBS-01 quote-race 409s, OBS-02 bounded 55P03, OBS-03 outbox expected rejection, OBS-05 403-by-design) so rules do not false-positive on expected business outcomes.
- **Dashboard templates**: admin-facing dashboard layout as JSON templates (or documented table-form dashboards) for the inventory — templates only.
- **OBS-04 observability acceptance**: the monitoring deliverable MUST demonstrate (host, on a reproduced stall or on the recorded reproduction evidence) that the OBS-04 indicator set detects idle-in-transaction/pool-saturation stalls. Monitoring makes the known High limitation visible; remediation remains the Command Center decision (D-070) — S7 records the dependency explicitly.

### 3.4 Rate limiting + Redis introduction (F-02) — production code, contract-authorized

- **Redis client dependency**: introduce a Redis client (decision O-2: `ioredis` vs `node-redis`; **this is the one authorized lockfile change of Phase 8** — F-02 explicitly requires Redis, so `pnpm-lock.yaml` changes are in-scope and must be minimal + CI re-validated; do NOT touch the lockfile for anything else).
- **Module boundaries** (PostgreSQL remains source of truth — non-negotiable):
  1. **Distributed rate limiter** — `RedisRateLimiter` implementing the existing `RateLimitPort` (`apps/api/src/auth/rate-limit.port.ts`), preserving the frozen CONFIGURABLE ceilings (10/IP/300s, 5/email/300s and any other frozen limiter configs — values stay in versioned config, never hard-coded). Closes AHS-003/D-019-C. `InMemoryRateLimiter` remains for dev/test where Redis is absent — provider selection must degrade cleanly (no crash without Redis; documented behavior).
  2. **Distributed lock** — behind a small lock port; **must NOT replace or weaken PG advisory locks on any financial path** (transaction confirm, MCP/iPoint adjustments, reconciliation runs, redemption order). Redis locks are for cross-instance coordination on non-correctness surfaces only (e.g., job scheduling de-dupe, cache invalidation) — the brief does NOT authorize Redis lock usage on financial write boundaries; any such proposal is an O-item.
  3. **Queue** — Redis queue **behind a port for non-critical dispatch only**; the outbox worker (`transaction-commission-outbox.worker.ts`, PG-based exactly-once, S6-proven drain 60→0) stays PG-backed and untouched. PostgreSQL remains the source of truth for all business state.
- **Integration tests (contract AC: "Redis integration tests (limiter/lock)")**: fail-closed guarded suite — `P8S7_DESTRUCTIVE_TEST` opt-in + dedicated `ipoint_p8s7_*` DB + Redis (CI already provisions `redis:7-alpine` in the api-integration job with `REDIS_URL`; host runs a local Redis or the S6-style compose container). Assert: limiter correctness across keys/windows (incl. ceiling parity with InMemory), lock mutual exclusion + expiry + release, queue enqueue/dequeue ordering, graceful degradation when Redis is unreachable (limiter falls back / fails per documented policy — no crash, no correctness impact on financial paths).
- **CI wiring**: additive steps in the existing api-integration job (redis service already there) following the S1–S4/S6 fail-closed pattern — never altering existing guards/DBs/job structure beyond additive extension. Full-repo CI must stay green at S7 close.

### 3.5 Runtime validation / dependency checks / environment validation / health-readiness (verify + bounded extension)

- **Runtime validation**: verify startup-time config validation exists and covers the S7-relevant env surface (`DATABASE_URL`, `REDIS_URL`, `AUTH_OTP_PEPPER`-class values, `REDEMPTION_VOUCHER_ENCRYPTION_KEY`); document the validated set; any gap = bounded fix or documented (severity-classified).
- **Dependency checks**: run `pnpm audit` (or the repo's standard check) at Phase 8 state + document the vulnerability posture (0 critical/high target; findings severity-classified; no dependency upgrade outside a written decision — frozen lockfile discipline except the authorized Redis addition).
- **Environment validation**: verify the environment matrix CI expects (Node 24 ubuntu vs Node 26 host parity note from S6) and the env-var contract in `.env.example` matches what code actually reads (no env invented in code that `.env.example` does not document, no secrets in `.env.example`).
- **Health/readiness endpoints** (contract AC): verify `GET /health/live` + `GET /health/ready`; **bounded extension** (contract-authorized production change) to include dependency checks in readiness — PostgreSQL connectivity (and Redis connectivity when configured) — returning per-check status without leaking credentials/connection strings; update the health spec accordingly; zero-bypass re-scan over the changed scope. If the executor judges the health controller frozen (S5e controller map: PUBLIC/NONE guard class), record the boundary decision (O-7) instead of silently rewriting.

### 3.6 Recovery procedures / operator & incident runbooks / release checklist (documentation)

- **Recovery procedures**: step-by-step restore/rollback procedures (DB restore, app rollback-to-commit, forward recovery per ops doc §3), each with expected outcome + verification step; ledger reconciliation after recovery (ops doc §8) referencing the P8-S2 reconciliation run.
- **Operator runbooks**: routine operations — migration apply, seed, backup/restore, worker health, Redis health, log locations, metric queries (S6-proven commands).
- **Incident runbooks** (ops doc §10 severity levels + the seven named incident classes: security incident, incorrect ledger balance, duplicate transaction, settlement failure, payment/top-up mismatch, data exposure, production outage): each with detection signal (ties to §3.3 alert rules), severity, containment, correction, reconciliation, root-cause + preventive-action record template.
- **Release checklist**: pre-release gate list mirroring ops doc §12 + P8-S9 final-gate matrix (migrations, builds, tests, OpenAPI, RBAC, secret scan, dependency scan, backup/restore evidence, monitoring/alert readiness, runbook references) — a checked-in checklist doc the P8-S9 verifier can execute against.

### 3.7 Security readiness (evidence + report)

- **Relationship to closed SEC work**: narrative table — P2-S4 AHS-001 fixed / AHS-002..004 documented (AHS-003 closed by S7 Redis limiter), SEC-01/SEC-02/P6-R2 frozen owners untouched, P7-S10 gates 19/20 baseline, S5e zero-bypass baseline, S6 FIX-001..004 security-relevant fixes — establishing the "already closed" security posture S7 extends.
- **Remaining scans**: re-run at Phase 8 state — (a) **secret scan** (P7-S10 gate-19 method / P8-S0 pattern) across the full repo **including S7 evidence files, test fixtures and committed templates**: 0 findings required (contract AC "secret scan clean"); (b) **zero-owner-bypass re-scan** over all S7-changed production code (Redis module, health, limiter) — 0 findings (S5e method, `scan-zerobypass.mjs`); (c) dependency scan (§3.5); (d) any remaining known scan surface (e.g., permission catalog untouched — cite S5e 49/49).
- **New scanning tooling**: default = reuse in-repo scanners + built-in `pnpm audit` (no new tool dependency without a decision — O-4). External tools (gitleaks/osv-scanner class) only via an explicit OpenClaw/Command Center decision.
- **No secrets discipline**: production credentials are deployment blockers (contract §7), never deliverables; test fixtures/evidence use placeholder values (existing `test_otp_pepper_32_chars_minimum_required`-class CI values); no real credentials, no private keys, no `.env` in any commit; `.npmrc` never committed.

### 3.8 Code/test deliveries vs documentation deliveries

| Type | Items |
|---|---|
| Code (production, contract-authorized, executor-class-gated) | `apps/api/src/redis/**` (Redis module: `RedisRateLimiter` on `RateLimitPort`, lock port, queue port, config + graceful degradation), health readiness extension (§3.5), auth rate-limiter provider wiring (§3.4) — **Codex CLI preferred; D-060 alternate only** |
| Code (test tooling) | `apps/api/src/redis/redis.integration.spec.ts` (limiter/lock/queue, fail-closed `P8S7_DESTRUCTIVE_TEST`), health spec update, additive CI steps (api-integration job) |
| Code (host-run ops scripts, no API surface) | backup/restore + migration-rehearsal + monitoring-probe scripts under `scripts/p8-s7/**` (documented, reproducible, secret-free; raw outputs to gitignored `.local/p8-s7-*/`) |
| Documentation | `TASK_BRIEF_P8S7.md` (this brief), `P8_S7_BACKUP_RESTORE_DR_REPORT.md`, `P8_S7_MIGRATION_REHEARSAL_REPORT.md`, `P8_S7_MONITORING_ALERTING_REPORT.md` (+ alert-rule + dashboard templates), `P8_S7_SECURITY_READINESS_REPORT.md`, `P8_S7_OPS_RUNBOOKS_RELEASE_CHECKLIST.md`, delivery report `P8_S7_DELIVERY_REPORT.md`; gate record filed by OpenClaw |
| Evidence artifacts | raw outputs under gitignored `.local/p8-s7-*/` — summarized (never pasted wholesale) into the reports with per-claim references |

## 4. Safety gates (zero-bypass / secret scan / fail-closed / no-deployment)

1. **Zero-owner-bypass**: any S7 production code must not instantiate or bypass frozen owners (Phase 1–7 owners + SEC-01/02 + P6-R2 routes + P8-S1..S4 canonical owners `ads-content`, `admin-reconciliation-ops`, `admin-risk-controls`, `admin-report-ops` extensions). Re-run the S5e zero-bypass scan over the S7-changed scope → **0 findings**; otherwise cite the S5e 445-file baseline as unchanged.
2. **Secret scan clean**: full-repo secret scan at Phase 8 state including S7 evidence/templates → **0 findings**; no production credentials, no private keys, no `.env`, no `.npmrc` in any commit (P7-S10 gate-19 pattern).
3. **Fail-closed spec guards**: every new S7 spec with DB writes carries `P8S7_DESTRUCTIVE_TEST` opt-in + dedicated `ipoint_p8s7_*` DB (S1–S4/S6 pattern); never `ipoint_ci`, never shared dev DBs. Redis integration tests require the Redis service (CI-provisioned) or host Redis; a spec that cannot reach Redis must skip cleanly (fail-closed for destructive, honest skip otherwise) — never crash the suite.
4. **No-deployment / no-prod-touch gate**: no production deployment, no production database access, no production credentials — every S7 "restore/backup" artifact is rehearsal-on-test-DB evidence; production backup/monitoring deployment is documented as a **deployment blocker** (as the contract classifies production credentials), not performed.
5. **Correctness gate**: Redis additions must not alter any PG-based correctness mechanism (advisory locks, idempotency, outbox exactly-once, reconciliation `withIdempotency`); no business-logic change; no migration; no benchmark/cosmetic-driven change.
6. **RBAC/permissions**: no new permission codes, no new API routes (health extension is an existing route's check payload only), no export surfaces.

## 5. Do Not Touch (frozen / prohibited)

- ❌ Frozen owners: Phase 1 merchant/MCP, Phase 2 member/auth, Phase 3 wallet/reward, Phase 4 transaction, Phase 5 agent/commission, Phase 6 redemption, Phase 7 admin-ops canonical owners + SEC-01/02 + P6-R2 hardened routes, P8-S1..S4 canonical owners. Delegation/adapter pattern only. `RateLimitPort` interface is the sanctioned integration seam — do not rewrite the auth module beyond provider wiring.
- ❌ Migrations 0000–0039 + `checksums.json` (**40/40 frozen**). No new migration, no checksum edit, no drift — rehearsals are read-only evidence runs.
- ❌ Outbox worker and every PG-based financial correctness mechanism (advisory locks, idempotency keys, exactly-once paths, reconciliation `withIdempotency`) — Redis queue/lock must not replace or wrap them.
- ❌ No weakening/skipping of existing guards or tests; no deletion of tests; no TypeScript strictness reduction; no lint/format config changes; no `.npmrc` committed.
- ❌ No production deployment, no production DB access, no production credentials anywhere (repo, fixtures, evidence, templates).
- ❌ No merge to `main`, no push to `main`, no force-push, no rebase/amend/reset --hard/clean/stash; forward-only integration via merge; exact-path staging only (no `git add .` / `-A`); untracked baseline (119) preserved — bulk-add prohibited.
- ❌ All committed files UTF-8 **without BOM** (D-061 L-1..L-3 precedent: BOM/em-dash encoding corruption bit previous sub-phases — verify bytes before commit; ASCII-safe punctuation preferred in all committed docs).

## 6. Definition of done (verifier will check)

1. `P8_S7_BACKUP_RESTORE_DR_REPORT.md` — backup procedure, **actual restore rehearsal evidence (backup → restore → verify)** with commands + verification outputs, RTO/RPO documented as targets, DR narrative, recovery procedures, deployment-blocker notes (production credentials/PITR). Every claim traceable to `.local/p8-s7-backup/**`.
2. `P8_S7_MIGRATION_REHEARSAL_REPORT.md` — fresh (0000–0039 on empty DB) + upgrade (0036 → 0039 staged) rehearsal evidence; checksums **40/40 unchanged**, drift clean after both.
3. `P8_S7_MONITORING_ALERTING_REPORT.md` — metric inventory (ops doc §7 categories + OBS-04 indicators), structured-logs/redaction verification result, committed **alert-rule templates** + **dashboard templates**, expected-outcome catalogue referenced (OBS-01/02/03/05 no-false-positive design), **OBS-04 observability demonstrated or explicitly evidenced** (indicator set fires on the recorded stall class), deployment note that this is template/host-validated form only.
4. `P8_S7_SECURITY_READINESS_REPORT.md` — SEC-relationship table, **secret scan clean (0 findings) at Phase 8 state incl. S7 files**, dependency check result (0 critical/high target; findings severity-classified), env/runtime validation, zero-owner-bypass re-scan over S7-changed scope (0 findings).
5. `P8_S7_OPS_RUNBOOKS_RELEASE_CHECKLIST.md` — recovery procedures, operator runbooks, incident runbooks (ops doc §10 classes + severity levels), release checklist executable by P8-S9.
6. Redis module committed behind service boundaries with fail-closed integration tests green (limiter/lock/queue + graceful degradation); rate-limiter ceilings preserved (config-sourced, not hard-coded); PG correctness mechanisms untouched; additive CI steps green in the api-integration job (redis service already provisioned); full-repo CI green at S7 close.
7. Health/readiness verified; bounded extension (DB + Redis checks when configured) done only per §3.5/§7 boundary decision, with spec updated and zero-bypass re-scan clean.
8. Severity-classified discrepancy log: **0 Critical / 0 High / 0 Medium required for approval**; Lows documented; OBS-04 recorded as an S7 **observability requirement met + remediation dependency** (not re-classified, not hidden).
9. Zero Do-Not-Touch violations; scoped conventional commits only (`docs(p8-s7): …`, `feat(p8-s7): …` for the Redis module, `test(p8-s7): …`, `ci(p8-s7): …`, `chore(p8-s7): …`); commit set = S7 files only; no `.npmrc`; UTF-8 no BOM; `git fsck` clean; tracked 0 modified outside the S7 commit set.
10. Executor provenance recorded (D-058 §0) and gate record `P8_S7_FINAL_GATE_RECORD.md` filed by OpenClaw (verifier), NOT by the executor; PHASE_REGISTRY/DECISION_LOG updated by OpenClaw only.

## 7. Deliverables

- `docs/06-phase-reports/p8-s7/TASK_BRIEF_P8S7.md` (this brief)
- `docs/06-phase-reports/p8-s7/P8_S7_BACKUP_RESTORE_DR_REPORT.md`
- `docs/06-phase-reports/p8-s7/P8_S7_MIGRATION_REHEARSAL_REPORT.md`
- `docs/06-phase-reports/p8-s7/P8_S7_MONITORING_ALERTING_REPORT.md` (+ alert-rule templates, dashboard templates — committed alongside or embedded)
- `docs/06-phase-reports/p8-s7/P8_S7_SECURITY_READINESS_REPORT.md`
- `docs/06-phase-reports/p8-s7/P8_S7_OPS_RUNBOOKS_RELEASE_CHECKLIST.md`
- `docs/06-phase-reports/p8-s7/P8_S7_DELIVERY_REPORT.md` (methodology, evidence summary, discrepancy log, bounded-fix records if any, deployment-blocker list, OBS-04 dependency statement, commit map)
- `apps/api/src/redis/**` — Redis module (limiter/lock/queue ports + client + graceful degradation) + `redis.integration.spec.ts` (fail-closed)
- `apps/api/src/auth/` provider wiring (rate-limiter selection) + `apps/api/src/health/` readiness extension + spec updates (bounded, §3.5)
- `scripts/p8-s7/**` — host-run backup/restore, migration-rehearsal, monitoring-probe scripts
- Optional additive `.github/workflows/p8-ci.yml` steps (api-integration job, redis service already present)
- `docs/06-phase-reports/p8-s7/P8_S7_FINAL_GATE_RECORD.md` — filed by OpenClaw (verifier), not by the executor

## 8. Notes / guidance for the executor

- **Read first**: `AGENTS.md`; `P8_S0_CONTRACT_FREEZE.md` §0/§7/§11/§12; `P8_S0_GAP_AUDIT_REPORT.md` (G-07 + F-02 + F-05 + F-08 rows); `P7-S10_FINAL_GATE_RECORD.md` (gates 19/20 methods); `docs/05-security/auth-security-review.md` (AHS-001..004); `docs/04-engineering/06_iPoint_Deployment_Security_and_Operations_V1.0.md` (§7/§8/§10/§12); `P8_S6_LOAD_PERFORMANCE_CONCURRENCY_REPORT.md` (§7 DB/worker observation methods, §9 OBS-01..05, §10 commands); `P8_S5E_DELIVERY_REPORT.md` (zero-bypass scan method); `P8_S5D_FINAL_GATE_RECORD.md` + `.github/workflows/p8-ci.yml` (fail-closed pattern + redis service); one S1–S4 spec for the `P8SN_DESTRUCTIVE_TEST` guard shape.
- **Evidence reuse principle**: P7-S10 gates 19/20 are the *historical* security baseline (S7 re-runs the scans at Phase 8 state — do not copy old results as current); S6 §7 observation methods are the *proven* indicator-measurement basis (reuse the commands, do not invent new measurement frameworks); S5e scan is the *current* zero-bypass baseline (re-run only over S7-changed scope). The ops doc is the *authoritative template* for runbooks/release-checklist/RTO-RPO/monitoring categories — map S7 artifacts to its sections explicitly.
- **Executor-class boundary (D-058 §5)**: production code (Redis module, health extension, limiter wiring) is **Codex CLI ONLY** unless D-060 (Bryan-authorized alternate executor) applies to this dispatch. OpenClaw does not write production code. A→B→C: implementer → independent Reviewer B' → OpenClaw host integration gate.
- **Severity guide** (mirror S5e/S6): Critical = financial invariant / security bypass; High = broken contract a real user journey hits (e.g., limiter that leaks secrets, restore procedure that cannot restore, alert set blind to the known OBS-04 class); Medium = drift/observation with no current user-visible break; Low = cosmetic/documentation. Approval bar: 0 Critical / 0 High / 0 Medium.
- **Host/CI constraints**: host = Windows, Node 26.4.0, pnpm 9.15.9, PostgreSQL 17 (`127.0.0.1:55440` P4-S7 env or S6 compose container `55432`), Redis via local service or S6-style compose; CI = ubuntu-latest Node 24 with postgres + redis services. Node 26/24 parity note applies (P4-S7 residual risk 2). Backup/restore/migration-rehearsal/OBS-04-probe runs are **host-only** (K-02-class); CI carries only the additive guarded suites + scans that are CI-safe.
- **Redis dependency (the authorized exception)**: F-02 makes the Redis client the one sanctioned Phase 8 lockfile change. Keep it minimal (one client lib + types); re-run the full p8-ci matrix after adding it; do not use the lockfile exception for anything else (S6 O-1 zero-new-dependencies discipline applies to everything except Redis).
- **Frozen limiter ceilings**: 10/IP/300s, 5/email/300s and any other frozen limiter values live in versioned config — the Redis limiter reads the same config; parity test against `InMemoryRateLimiter` is part of the integration suite.
- **OBS-04 handling — precise boundary**: S7 delivers *observability of* OBS-04 (indicators, alert templates, host probe demonstration, dependency statement in the delivery report). S7 does **NOT** patch reconciliation engine code or connection-pool discipline to fix it (out of scope; Command Center decision per D-070). If the executor believes a fix is unavoidable for the monitoring demonstration, that is an O-item — escalate, do not self-authorize.
- **Raw evidence**: keep raw outputs under gitignored `.local/p8-s7-*/` — never commit raw logs; reports summarize and reference. Never fabricate numbers; a failed/absent measurement is reported as such (honesty contract, P4-S7/P8-S4/S6 precedent).
- **Migration/checksum discipline**: confirm `git diff` shows no `packages/database/**` change in the final commit set; rehearse, do not touch.
- **Provenance**: executor class/model/branch/commits recorded in `EXECUTOR_PROVENANCE_REGISTER.md` by OpenClaw at gate time — keep commits scoped and messages conventional.

## 9. Open decision points (surface to OpenClaw / Command Center)

- **O-1 — Backup/restore rehearsal environment**: default = host PostgreSQL with dedicated `ipoint_p8s7_source_*` / `ipoint_p8s7_restore_*` test DBs (full dump → restore → verify, including a functional write-path smoke on the restored DB). Confirm this host-PG real-rehearsal is authorized (the task frame allows host rehearsals; production backup remains a deployment blocker). Alternative = dump/restore on the CI postgres service (ephemeral; weaker evidence). Recommend host rehearsal + CI smoke only if cheap.
- **O-2 — Redis client library + module scope**: `ioredis` vs `node-redis` (+ version) — the authorized lockfile change; also confirm the module boundary list (rate limiter + lock + queue ports) and that **Redis lock/queue are NOT applied to any PG-correctness path**. If the executor sees a strong case for Redis on a financial-adjacent surface, escalate (do not self-authorize).
- **O-3 — Monitoring delivery form**: default = committed alert-rule/dashboard **templates** + metric inventory + host-validated probes (no new runtime metrics dependency, no deployed infra — self-consistent with no-deployment). Alternative = add a metrics dependency (prom-client class) for an in-app /metrics endpoint — that is a lockfile + runtime surface decision → escalate; do not add silently.
- **O-4 — Security scan tooling**: default = reuse in-repo secret/zero-bypass scanners + built-in `pnpm audit`. If gitleaks/osv-scanner-class external tooling is judged materially necessary, that is a decision (CI + dependency impact) → escalate, do not self-authorize.
- **O-5 — RTO/RPO + alert threshold values**: CONFIGURABLE values, not hard-coded. S7 documents proposed targets/thresholds as proposals; Bryan/Command Center sign-off on final production RTO/RPO and alert thresholds is a production-launch policy decision, not an S7 invention.
- **O-6 — OBS-04 observability acceptance + remediation dependency**: confirm S7 acceptance = monitoring that *detects* the OBS-04 class (host demonstration against the recorded stall evidence) while remediation stays with the Command Center decision (D-070 paths). S7 must not absorb an OBS-04 fix into its scope.
- **O-7 — Health controller extension boundary**: readiness currently checks config only; contract AC wants health/readiness endpoints (exist) + runtime/dependency validation. Default = bounded extension adding DB (+ Redis when configured) checks behind the existing route, with spec + zero-bypass re-scan. If the health controller is treated as frozen (S5e PUBLIC/NONE guard class), record the decision and deliver verification-only. OpenClaw picks at dispatch; default per this brief is the bounded extension.

_Forward-only brief. Do not delete or rewrite. Superseded only by a bounded addendum recorded in the P8-S7 gate record._
