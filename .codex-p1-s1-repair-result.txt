```markdown
# P1-S1 Repair Order 完成报告

## 1. Summary
已完成 CR-01 至 CR-07，并通过 CR-08 跨文档一致性检查。所有修改均限制在 `docs/06-phase-reports/p1-s1/`。

## 2. Files changed
- PHASE_1_ACCEPTANCE_PLAN.md
- PHASE_1_API_AND_PERMISSION_MATRIX.md
- PHASE_1_BASELINE_AUDIT.md
- PHASE_1_DEFERRED_ITEMS.md
- PHASE_1_DOMAIN_MAP.md
- PHASE_1_ERD_PROPOSAL.md
- PHASE_1_MCP_LEDGER_INVARIANTS.md
- PHASE_1_SCHEMA_AND_MIGRATION_PLAN.md
- PHASE_1_SCOPE_MATRIX.md
- PHASE_1_STATE_MACHINES.md

## 3. What was implemented
- 建立正式 MerchantGroup 实体及 Account → Group → Branch 所有权关系。
- 移除 MerchantBranch.account_id 唯一关系。
- 拆分 Application、KYC、Operational 三套状态机。
- 完全移除 Phase 1 的 Receipt 表、枚举、端点和运行行为提案。
- Merchant primary email 固定为不可修改的 Account email。
- append-only 表移除 updated_at、soft-delete/archive 字段，并规定拒绝 UPDATE/DELETE。
- Special Percentage 保留 `numeric(12,6)` 与 `rate > 0`，不再虚构最大比例。
- 保持全部 `implementation_authorized: false`。

## 4. Product / business value
消除了商家集团、多分店所有权、审核状态、邮箱身份和不可变财务证据之间的架构歧义，降低后续 P1-S2 出现错误 schema、权限越界或业务规则硬编码的风险。

## 5. Complexity or maintenance risk
风险较低，仅修改规划文档。MerchantGroup 采用直接 Account ownership，避免提前引入员工角色、集团权限、共享 MCP 或集团结算复杂度。

## 6. Tests / verification run
- 10/10 Markdown 文件存在且 frontmatter 正常。
- 10/10 `implementation_authorized: false`。
- `git diff --check`：PASS。
- 相对 Previous SHA 的变更仅包含授权目录内 10 个文件。
- banned legacy terms：0。
- Receipt 出现次数：1，仅为 DEFERRED_ITEMS.md 指定声明。
- 所有指定 append-only ERD 表均无 mutation/soft-delete 字段。
- 本地、tracking 和 GitHub remote SHA 完全一致。
- 授权工作树干净，无 tracked 或 untracked 残留。

## 7. Results
最终远端 SHA：

ffb8418b2d5d711e26deed6a345a2af6e48bd277

Branch：

task/p1-s1-baseline-domain-map

Remote：

origin/task/p1-s1-baseline-domain-map

提交记录：
- `1f0ba5a` — docs(p1-s1): resolve command center architecture findings
- `ffb8418` — docs(p1-s1): resolve command center architecture findings

执行期间共享工作树的并发过程先提交并推送了第一批修改；剩余范围收敛使用普通后续提交完成，没有 amend 或 force push。

## 8. Known issues or limitations
- 没有独立提交的 P1 Phase Brief；本次 Command Center Repair Order 作为当前授权依据。
- Special Percentage 最大值、最小业务增量和审批阈值仍为 O-07 OPEN。
- 本次没有运行应用构建或代码测试，因为没有修改或授权任何生产代码。

## 9. Anything deferred
- Receipt and Transaction Engine fully deferred。
- P1-S2 未授权。
- Merchant Group UI、集团权限、共享 MCP、集团结算及集团报表未实现。
- Public support email 保留为未来独立产品决策。
- 无生产 schema、migration、API、UI 或外部副作用。

## 10. Next recommended step
由 ChatGPT Command Center 审阅远端 SHA `ffb8418b2d5d711e26deed6a345a2af6e48bd277`，确认 P1-S1 修复结果；在正式授权前不要启动 P1-S2。
```