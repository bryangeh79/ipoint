```markdown
# P1-S2 交付报告

## 1. Summary

P1-S2 Merchant Schema and Migrations 已实现、提交并推送。

- Phase 分支：`phase/1-merchant-onboarding-mcp`
- Task 分支：`task/p1-s2-merchant-schema`
- 远端 Task HEAD：`b4a44ea87f199d39831eb784d91b5d2b5f65cbca`
- 未合并或推送 `main`
- 未实施 P1-S3、P1-S4、P1-S5+
- tracked 工作区干净，既有未跟踪资料未被修改、清理或提交

## 2. Files changed

- `docs/06-phase-reports/p1-s1/`：修正 SpecialPercentage 规则并统一已跟踪文档格式
- `packages/database/schema/index.ts`
- `packages/database/migrations/0002_phase_1_merchant_package_mcp.sql`
- `packages/database/migrations/checksums.json`
- `packages/database/seeds/foundation.ts`
- `packages/database/src/expected-schema.ts`
- `packages/database/tests/schema.unit.test.ts`
- `packages/database/tests/database.integration.test.ts`

## 3. What was implemented

- 10 个 PostgreSQL/Drizzle enum
- 25 个 Merchant、Package、MCP 相关表，包括独立 gallery metadata 和 Merchant ID counter
- Merchant Application、KYC、document、terms、status history、MCP ledger 和 adjustment decision append-only triggers
- SpecialPercentage 与标准费率约束：`numeric(12,6)`、`rate > 0 AND rate <= 100`
- Service fee effective-range exclusion constraint
- Package assignment XOR、唯一 active default 与 deferred trigger
- MCP balance、amount、entry type、idempotency、reversal self-FK 等约束
- Account email 数据库级不可变 trigger
- 按 market/channel 原子分配 Merchant ID：`my_of_000001`
- A–F 标准 package rate seeds：2.5、5、10、15、20、25
- 15 个 Phase 1 permission seeds
- Fresh migration、0001 upgrade、重复 migration、seed idempotency、drift 和 checksum 测试

## 4. Product / business value

建立 Merchant onboarding、KYC、package 管理及 MCP 财务基础，确保审批证据不可篡改、商业费率精确、Merchant ID 并发安全，并为后续 P1-S3/P1-S4 提供稳定数据库契约。

## 5. Complexity or maintenance risk

- `0002` 是较大的单一 migration，但遵循授权要求和依赖顺序。
- 使用 `btree_gist` 支持 effective-range exclusion constraint。
- Package active-default 使用 deferred constraint trigger，应用事务必须维持一个 active default。
- 已应用 migration 不可修改；后续问题必须通过 forward-fix migration 处理。

## 6. Tests / verification run

通过：

- Scoped Prettier：PASS
- Scoped ESLint：PASS
- `pnpm typecheck`：PASS
- `pnpm build`：PASS
- `pnpm test`：55 passed
- Database unit：5/5 passed
- PostgreSQL integration：15/15 passed
- Fresh `0000 → 0001 → 0002`：PASS
- Isolated `0001 → 0002` upgrade：PASS
- Migration idempotent rerun：PASS
- Append-only UPDATE/DELETE rejection：PASS
- Concurrent Merchant ID generation：25/25 唯一
- Seed twice：PASS，数量不变
- `pnpm db:checksum`：3 checksums verified
- `pnpm db:migrate`：PASS
- `pnpm db:drift`：PASS
- Email immutability：PASS
- Secret scan：NONE
- P1-S3+ scope leakage scan：NONE

## 7. Results

提交：

- `8e48471591783e991635330307f2e36d7cd73689` — `docs(p1-s1): lock special percentage range`
- `fe341fa9b7949d24eaf256afea6aa460f229c112` — `style(p1-s1): format phase planning docs`
- `b4a44ea87f199d39831eb784d91b5d2b5f65cbca` — `feat(database): add phase 1 merchant package and mcp schema`

本地与远端 Task HEAD 完全一致。

## 8. Known issues or limitations

全仓 `pnpm format:check` 和 `pnpm lint` 仍被既有未跟踪文件阻挡：

- 6 个未跟踪 policy/scripts 文件未通过 Prettier
- 未跟踪 `scripts/close-phase0.mjs` 有 2 个 `console is not defined` lint 错误

这些文件受治理规则保护，未修改或提交。授权范围内文件的 format 和 lint 均通过。

## 9. Anything deferred

- Merchant onboarding API/service：P1-S3
- KYC review业务服务：P1-S4
- Receipt、QR、Transaction、Reward、Commission、Advertising
- Merchant staff/sub-account
- 真实支付、退款、云储存、通知及外部 provider integration
- `main` merge

## 10. Next recommended step

提交给 ChatGPT Command Center 审查 P1-S2 commit 与 PostgreSQL 测试证据。获批后才按顺序进入 P1-S3；未跟踪脚本的全仓 format/lint 问题应另行授权处理。
```